default:
  image: openjdk:18-alpine

stages:
  - build
  - test

build:
  stage: build
  script:
    - ./gradle build
  artifacts:
    paths:
      - build/libs/*.jar

test:
  stage: test
  script:
    - ./gradlew test